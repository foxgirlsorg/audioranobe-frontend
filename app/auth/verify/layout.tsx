import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Подтверждение почты — AudioRanobe',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
