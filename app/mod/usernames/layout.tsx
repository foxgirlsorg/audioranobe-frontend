import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Зарезервированные имена — модерация — AudioRanobe',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
