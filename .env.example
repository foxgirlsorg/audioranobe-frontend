# AudioRanobe frontend — copy to .env and edit.

# Public URL of the backend API (as seen from the user's browser)
NEXT_PUBLIC_API_URL=http://your-backend-host:8080/api

# Port to expose the frontend on
FRONTEND_PORT=3000
